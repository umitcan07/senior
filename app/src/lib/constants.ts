export const THEME_STORAGE_KEY = "nounce-theme-preference";

/**
 * localStorage key prefix for the diff-viewer view mode. The full key is
 * suffixed with the comparison type (`-phoneme` / `-word`) so the phoneme and
 * word toggles persist independently across sessions.
 */
export const DIFF_VIEW_STORAGE_KEY_PREFIX = "nounce-diff-view";
