# RunPod serverless worker
runpod>=1.0.0

# Core dependencies for acoustic model inference
# GPU support via CUDA (install appropriate PyTorch version for your CUDA version)
# For RunPod: PyTorch with CUDA support is typically pre-installed
torch==2.0.1

# Audio processing
librosa
soundfile
numpy<2

# ESPnet / POWSM (Speech-to-Text / Phone Recognition)
espnet==202511
espnet_model_zoo==0.1.7

# Montreal Forced Aligner (Alignment)
montreal-forced-aligner>=2.2.17
pgvector>=0.2.0

# HTTP client for downloading audio files
requests>=2.31.0
