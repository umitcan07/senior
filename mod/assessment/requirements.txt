# Core requirements matching working environment:
# - torch: 2.4.0+cu118 (installed separately in Dockerfile)
# - espnet: 202412
espnet==202412
espnet-model-zoo==0.1.7
# torch and torchaudio installed separately in Dockerfile with CUDA support
runpod>=1.0.0
soundfile
requests
# Silero VAD for no-speech abstention (E3.1 / #18). CPU-only, runs in tens of ms.
silero-vad
# LoRA adapter loading for the fine-tuned Turkish PR model.
# Pin transformers to 4.x: peft 0.13.2 and espnet 202412 are both built against
# the transformers 4.x API. Without this pin pip pulls transformers 5.x, which
# breaks peft's import (BloomPreTrainedModel lazy-import error).
transformers==4.46.3
peft==0.13.2
# Articulatory feature table for the feature-weighted phone diff (E7.6 / #57,
# mod/phone_features.py). CPU-only, pure-Python + a small bundled IPA CSV; the cost
# is a memoized pure function so there's no per-request latency. On Linux the worker
# image builds it cleanly; the Windows dev host needs the --no-deps install in
# mod/dev/requirements-dev.txt.
panphon
