# RunPod serverless worker
runpod>=1.0.0

# Core dependencies for acoustic model inference
# Upgrading to modern PyTorch for compatibility with espnet 202511
torch>=2.0.0
torchaudio

# Audio processing (optional, for audio-guided G2P)
librosa
soundfile
numpy

# ESPnet / POWSM (G2P)
# Using latest version (202511) with modern PyTorch
espnet==202511
espnet_model_zoo==0.1.7

# HTTP client for downloading audio files (if audio_uri provided)
requests>=2.31.0
